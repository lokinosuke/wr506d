import * as pg from 'pg';
import { Adapter } from './adapter';
export declare class Client extends Adapter {
    private client;
    private done?;
    readonly pooled: boolean;
    constructor(client: pg.Client, done?: Function);
    /**
     * End connection
     */
    end(): void;
    /**
     * Run query and return raw result set
     */
    run(text: pg.QueryConfig | string, values?: any[]): Promise<pg.QueryResult>;
    /**
     * Prepare a query
     */
    prepare(text: string): {
        run: (values?: any[]) => Promise<pg.QueryResult>;
        all: (values?: any[]) => Promise<any[]>;
        one: (values?: any[]) => Promise<any>;
    };
}
