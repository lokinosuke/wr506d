import * as pg from 'pg';
import { Sql } from './sql';
export declare abstract class Adapter {
    sql: Sql;
    constructor();
    /**
     * Run query and return raw result set
     */
    abstract run(text: pg.QueryConfig | string, values?: any[]): Promise<pg.QueryResult>;
    /**
     * Get all rows from result set
     */
    all(text: pg.QueryConfig | string, values?: any[]): Promise<any[]>;
    /**
     * Get first row from result set
     */
    one(text: pg.QueryConfig | string, values?: any[]): Promise<any>;
}
