import * as pg from 'pg';
import { Client } from './client';
import { Connection } from './connection';
export declare class Sql {
    private connection;
    static format(strings: string[], ...params: any[]): pg.QueryConfig;
    constructor(connection: Client | Connection);
    format(strings: string[], ...params: any[]): pg.QueryConfig;
    run(strings: string[], ...params: any[]): Promise<pg.QueryResult>;
    all(strings: string[], ...params: any[]): Promise<any[]>;
    one(strings: string[], ...params: any[]): Promise<any>;
}
